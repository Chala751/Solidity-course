# Basic Data Types

## Booleans

See Booleans.sol for the example contract.

## Unsigned Integers

See UnsignedIntegers.sol for the example contract.

## Signed Integers

See SignedIntegers.sol for the example contract.

## String Literals

See StringLiterals.sol for the example contract.

## Enum

See Enum.sol for the example contract.

## Required Installation

Option 1: Use Remix (no local install)
- Open https://remix.ethereum.org/
- Create Booleans.sol and paste the code
- Compile with the Solidity compiler version 0.8.20

Option 2: Compile locally with solc
- Install Node.js (LTS) from https://nodejs.org/
- From this folder, run:
  - npm init -y
  - npm install --save-dev solc
- Compile:
  - npx solc --version
  - npx solc Booleans.sol
